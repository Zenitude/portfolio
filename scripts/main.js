// Récupération du body / Body recovery
const body = document.body;

// Création du container principal / Creation of the main container
const containerPrincipal = document.createElement('div');
containerPrincipal.setAttribute('class', 'container');
body.appendChild(containerPrincipal);
